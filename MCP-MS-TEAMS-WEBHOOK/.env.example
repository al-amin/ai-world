# MS Teams Webhook Configuration
TEAMS_WEBHOOK_URL=https://outlook.office.com/webhook/your-webhook-id/IncomingWebhook/your-webhook-path
TEAMS_WEBHOOK_NAME=default
TEAMS_WEBHOOK_DESCRIPTION=Default MS Teams webhook

# Alternative: Path to a JSON configuration file with multiple webhooks
# TEAMS_WEBHOOK_CONFIG_FILE=/path/to/webhook_config.json

# MCP Server Configuration
MCP_TRANSPORT=stdio  # stdio or sse
MCP_PORT=8000  # Only used with sse transport
MCP_LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL